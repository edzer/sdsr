# (PART) Spatial Data {-}

# Hello, world! {#intro}

hello world, spatial data

```{r eval=TRUE, fig=TRUE}
library(tidyverse)
library(sf)
system.file("gpkg/nc.gpkg", package="sf") %>%
	read_sf %>%
	select(NWBIR74) %>%
	plot
```
